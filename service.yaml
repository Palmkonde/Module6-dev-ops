# Expose port 
apiVersion: v1
kind: Service
metadata:
  labels:
    app: myapp

spec:
  selector:
    app: myapp
  type: ClusterIP 

  ports:
    - port: 4444
      protocol: TCP
      targetPort: 4444

